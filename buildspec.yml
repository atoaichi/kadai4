version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name CF-public-1a --s3-location s3://ope-kadai4-s3/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src
  